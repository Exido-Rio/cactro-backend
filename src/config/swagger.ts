/**
 * This file is no longer needed for manual Swagger configuration.
 * TSOA now automatically generates the swagger.json file into the public directory
 * based on the decorators in the routing controllers.
 */
export const swaggerSpec = {};

